#include <stdio.h>

int main()
{
    int n;

    while (1)
    {
        printf("Digite N: ");
        scanf("%d", &n);
        if (n < 0)
        {
            break;
        }
        else
        {
        }
    }

    return 0;
}