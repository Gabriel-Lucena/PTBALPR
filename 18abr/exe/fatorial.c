#include <stdio.h>

int main()
{

    int numero, contador = 0, afk = 0;

    printf("Digite o número: ");
    scanf("%d", &numero);

    numero = contador;

    while (contador < 2)
    {
        op = numero * 

        numero *= (numero - contador);

        contador--;
    }

    printf("%d", numero / );

    return 0;
}